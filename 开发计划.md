# 九章写作 — 开发计划（排除积分系统）

## 总览

基于 PRD v1.0.0 和当前代码状态，排除积分系统后，剩余 8 个功能模块按三批推进。每批内部按依赖关系排序，一批跑通再进下一批。

---

## 第一批：编辑器核心体验（直接影响每次写作）

### 1.1 AI 纠错 + 去 AI 味

**PRD 要求：**
- AI纠错：基础文本规范、敏感词与内容合规性、创作逻辑与一致性。内容大于100字才可用。
- 去AI味：以"10年网文主编"口吻重写。大纲/章节均可调用。
- 结果通过 SSE 流式返回，支持复制/重新生成/点赞/点踩。

**当前状态：**
- 后端有 `/api/ai/detect` 但只做机械表达检测，不满足 PRD 的全维度审计要求。
- 缺少专门的去 AI 味路由。
- 前端编辑器 toolbar 有视觉占位按钮但无实际调用。

**开发内容：**
- 后端 `backend/src/routes/ai.ts`：
  - 增强 `POST /api/ai/detect`：system prompt 改为全维度审计（逻辑架构 + 文本细节 + 敏感词 + 文风一致性），增加 `minLength` 校验（100字）。
  - 新增 `POST /api/ai/de-ai`：system prompt 为"10年网文主编去AI味"，支持 SSE 流式。
- 前端 `frontend/js/app.js`：
  - 编辑器 toolbar「AI纠错」「去AI味」按钮绑定点击事件。
  - 点击后在 AI 对话面板弹出对应提示词并自动发送。
  - 如果当前是正文页且字数<100，点击纠错时 toast 提示。

**预估工作量：** 4h

---

### 1.2 查找替换

**PRD 要求：**
- 点击替换按钮，右上角展示查找替换弹窗（不遮挡编辑页）。
- 可替换本页/全部。
- 支持正则可选。

**当前状态：** 完全缺失。

**开发内容：**
- 前端 `frontend/js/app.js` + `frontend/css/style.css`：
  - 新增查找替换弹窗组件（右上角浮动，含查找输入框、替换输入框、选项区）。
  - `查找下一个`：在编辑器 contenteditable 中高亮匹配文本。
  - `替换`：替换当前匹配项。
  - `全部替换`：替换所有匹配项。
  - `区分大小写`、`使用正则` 两个 checkbox。
  - 显示匹配数量（如：3/12）。
- 无后端改动。

**预估工作量：** 3h

---

### 1.3 导入作品（txt 格式）

**PRD 要求：**
- 点击导入作品按钮，弹出文件选择，仅支持 txt/word，文档名作为作品名称（最多15字）。
- 文件大小不超过 20M。
- 章节名称请独立一行，否则可能导入失败。
- 导入成功后以作品卡片展示，提示导入成功。

**当前状态：** 完全缺失。

**开发内容：**
- 前端 `frontend/js/app.js`：
  - 我的作品页「导入作品」按钮 + 文件选择弹窗。
  - 文件大小校验（≤20M）。
  - 读取 txt 内容，预览章节拆分结果（左侧章节列表可删除，右侧内容可修改）。
  - 确认后 POST 到后端。
- 后端 `backend/src/routes/works.ts`：
  - 新增 `POST /api/works/import`：接收 `{ title, chapters: [{title, content}] }`，创建作品 + 批量创建章节。
  - 章节拆分规则：以独立一行的"第X章"、"Chapter X"、"X、"等模式识别章节标题。
- word 格式（.docx）第一期先不做，预留接口。

**预估工作量：** 4h

---

## 第二批：作品管理流程

### 2.1 作品详情页

**PRD 要求：**
- 新建作品弹窗保存后，跳转到作品详情页（不是直接进编辑器）。
- 字段：作品名称、作品视角（第一/第三人称，单选必填）、频道（男频/女频/全频，单选必填）、标签（按频道动态拉取，最多2个，默认选第一个）、作品简介（非必填）、作品封面（第一期默认图）。
- 保存后跳转到编辑作品正文页；修改时回到我的作品页。

**当前状态：**
- 新建作品后直接跳编辑器，缺少详情页。
- 标签系统有 64 个固定标签，但没有按频道过滤。

**开发内容：**
- 前端 `frontend/js/app.js`：
  - 新建「作品详情页」路由和渲染函数。
  - 表单：名称输入框、视角单选、频道单选、标签多选（按频道过滤，最多2个）、简介 textarea、封面默认图。
  - 新建时：保存 → 跳转编辑器。
  - 修改时：保存 → 返回我的作品页。
  - 我的作品页「作品详情」菜单项点击进入。
- 后端 `backend/src/db/schema.ts`：
  - `works` 表新增字段：`perspective`（视角）、`channel`（频道，替代或补充 genre）。
  - 更新 `npm run db:push` 同步 schema。
- 标签按频道过滤逻辑：前端维护频道→标签映射表，根据选择的频道动态展示对应标签。

**预估工作量：** 5h

---

### 2.2 灵感库完整功能

**PRD 要求：**
- 灵感卡片：名称、来源（AI生成/热门榜单/自创）、标签（最多5个）、内容（最多显示5行）、编辑时间。
- 操作：删除（进回收站）、复制、根据灵感扩写（跳转作品详情并填入简介）。
- 记录灵感：名称/标签/内容，保存后停留在当前页。
- 分类筛选：全部、AI生成、热门榜单、自创。
- 搜索：模糊搜索名称/标签/内容。
- 回收站：恢复/永久删除/清空。
- 每页最多 30 条，按最后编辑时间倒序。

**当前状态：** 只有 4 张示例卡片，无后端支持。

**开发内容：**
- 后端 `backend/src/db/schema.ts`：
  - 新增 `inspirations` 表：id, userId, title, source（ai/trend/custom）, tags, content, deletedAt, createdAt, updatedAt。
  - `npm run db:push`。
- 后端 `backend/src/routes/`：
  - 新增 `inspirations.ts`：GET 列表（支持 source 过滤 + search 模糊搜索 + deletedAt 过滤）、POST 创建、PUT 更新、DELETE 软删除、POST restore、DELETE permanent、GET trash。
- 前端 `frontend/js/app.js`：
  - 灵感列表：真实数据加载、分页（每页30条）、分类 tab、搜索框。
  - 新建灵感页：表单 + 保存。
  - 灵感编辑页：点击卡片进入编辑。
  - 回收站页面。
  - 「根据灵感扩写」：跳转作品详情页并自动填入灵感内容到简介。

**预估工作量：** 6h

---

## 第三批：登录与榜单

### 3.1 手机号验证码登录

**PRD 要求：**
- 手机号输入框 + 验证码输入框 + 发送验证码按钮。
- 未注册手机号首次登录自动创建账户。
- 微信/飞书登录且未绑定手机号时，显示绑定手机按钮。

**当前状态：** 只有密码登录。

**开发内容：**
- 后端 `backend/src/routes/auth.ts`：
  - 新增 `POST /api/auth/send-code`：生成 6 位数字验证码，存入内存缓存（过期 5 分钟）。
  - 新增 `POST /api/auth/login-by-code`：验证验证码，通过则登录/自动注册。
  - 生产环境对接短信平台（第一期可先 mock，验证码固定为 123456）。
- 前端 `frontend/js/app.js`：
  - 登录页改造：密码登录 / 验证码登录 tab 切换。
  - 验证码登录表单：手机号 + 验证码 + 发送按钮（60秒倒计时）。
  - 注册页同步支持验证码注册。

**预估工作量：** 4h

---

### 3.2 飞书登录

**PRD 要求：**
- 点击飞书图标，打开飞书授权页面。
- 首次登录需用手机号登录绑定。
- 非首次登录授权后直接进我的作品页。

**当前状态：** 完全缺失。

**开发内容：**
- 后端 `backend/src/routes/auth.ts`：
  - 新增飞书 OAuth 流程：`GET /api/auth/feishu`（跳转授权）、`GET /api/auth/feishu/callback`（处理回调）。
  - 用户表增加 `feishuOpenId` 字段用于关联。
  - 首次飞书登录创建账户，但标记为未绑定手机；后续绑定手机后解除标记。
- 前端 `frontend/js/app.js`：
  - 登录页增加飞书图标按钮。
  - 绑定手机弹窗（飞书登录后未绑定手机号时弹出）。
- 需要申请飞书开放平台应用，获取 appId/appSecret。

**预估工作量：** 5h（依赖飞书开放平台申请）

---

### 3.3 热门榜单

**PRD 要求：**
- 平台热搜榜：抖音/微博/今日头条/百度/B站，50条（B站20条），可往前选7天。
- 书籍榜单：按在读人数排列，二级分类 tab，热门灵感（热词+风向分析+爆款灵感）。
- 九州榜单：根据热度值显示九州书籍库，5万字以内短篇小说。
- AI拆书（P1）。

**当前状态：** 只有 10 条硬编码趋势数据。

**开发内容：**
- 后端 `backend/src/index.ts` 或新增 `routes/trends.ts`：
  - 方案 A：爬虫定时抓取各平台热搜（需要定时任务 + 反爬处理）。
  - 方案 B：对接第三方热搜 API（如有）。
  - 方案 C（推荐第一期）：扩展硬编码数据，模拟各平台热搜结构，留出接口位。
- 前端 `frontend/js/app.js`：
  - 热门榜单页重构：一级 tab（平台热搜/男频热度/男频新书/女频热度/女频新书/九州榜单）。
  - 平台热搜：二级 tab（抖音/微博/今日头条/百度/B站）+ 日期选择下拉框。
  - 书籍榜单：二级分类 tab + 热门灵感展开/收起区 + 书籍卡片列表。
  - 搜索框：当前分类下模糊搜索。
- 书籍数据第一期可硬编码或从现有数据源导入。

**预估工作量：** 8h（如果做真实爬虫则更长）

---

## 执行顺序

```
第一批（编辑器核心体验）
  → 1.1 AI 纠错 + 去 AI 味
  → 1.2 查找替换
  → 1.3 导入作品（txt）

第二批（作品管理流程）
  → 2.1 作品详情页
  → 2.2 灵感库完整功能

第三批（登录与榜单）
  → 3.1 手机号验证码登录
  → 3.2 飞书登录（需先申请飞书开放平台）
  → 3.3 热门榜单
```

每批完成后跑一次端到端验证：创建作品 → 编辑 → AI 纠错/去 AI 味 → 导入 → 查看详情 → 灵感库。

---

## 验收标准（每批通用）

1. 改完主动跑验证：`cd backend && npm run dev`，打开 `http://localhost:3000` 走一遍功能链路。
2. 前端 console 无报错，后端无 500 错误。
3. 刷新页面数据持久化（数据库有写入）。
4. PRD 描述的字段和交互全部覆盖。
