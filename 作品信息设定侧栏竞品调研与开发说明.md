# 作品信息/设定侧栏竞品调研与开发说明

调研日期：2026-04-30  
提交对象：CC 开发  
参考对象：
- 蛙蛙写作：https://wawawriter.com/app/novel/center
- 星月写作：https://xingyuexiezuo.com/
- 参考截图：`/Users/mac1/Desktop/截屏2026-04-30 17.41.49.png`

## 1. 结论

截图中的模块高度匹配蛙蛙写作的小说编辑侧栏。蛙蛙的结构是：

```text
顶部一级 Tab：
作品信息 | 正文 | 草稿

作品信息内：
主题
  - 标签
  - 总纲

设定
  - 角色
    - 主要角色
    - 次要角色
  - 背景
  - 势力
  - 地点
  - 物品
  - 自定义分类
```

星月写作没有完全相同的“作品信息/正文/草稿”三 Tab 命名，但有对应能力：书籍信息、章节正文、角色库、词条库、备忘录、章节摘要、书籍摘要、快捷指令/工作流引用。适合做成同一套“作品资料库侧栏”，底层支持不同实体类型。

建议 CC 开发时优先复刻蛙蛙的侧栏体验，同时吸收星月的“角色/词条/备忘录目录化管理、批量移动、置顶、抽取角色/词条”的能力。

## 2. 蛙蛙写作：该部分功能拆解

### 2.1 顶部一级 Tab

| Tab | 作用 | 交互 |
|---|---|---|
| 作品信息 | 管理作品标签、总纲、角色、世界设定 | 点击后显示截图中的资料树 |
| 正文 | 管理章节/卷/正文内容 | 短篇/剧本显示章节管理；长篇显示卷、章节树 |
| 草稿 | 管理草稿内容 | 可恢复、继续编辑，具体内容需登录后验证 |

前端可验证逻辑：
- `作品信息` 下固定包含 `标签`、`总纲`、`角色`。
- 长篇作品额外包含 `设定`。
- `正文` 下根据作品类型显示 `章节` 或 `卷 + 章节`。
- `草稿` 作为独立顶部 Tab 保存草稿状态。

### 2.2 作品信息：主题区

| 菜单项 | 格式 | 功能 |
|---|---|---|
| 标签 | 标签列表 | 展示/编辑作品标签，如题材、时空、情绪、读者频道等 |
| 总纲 | 文本/思维导图 | 展示作品总纲；支持文本总纲与思维导图两个视图 |

总纲数据格式建议：

```json
{
  "id": "outline_id",
  "title": "总纲",
  "content": "<p>富文本总纲内容</p>",
  "related_roles": null,
  "foreshadowings": []
}
```

蛙蛙前端里总纲类型包含：
- `GENERAL_OUTLINES`：总纲
- Tab：`文本总纲`、`思维导图`
- 默认 `hiddenLeftChild: true`，即总纲在左侧不一定展开子项

### 2.3 作品信息：设定区

截图中“设定”右侧有 `+`，表示可新增设定项或新增分类。蛙蛙支持：

| 分类 | 内置/自定义 | 子项格式 | 操作 |
|---|---|---|---|
| 角色 | 内置 | 主要角色、次要角色 | 新增、展开/收起、预览 |
| 背景 | 内置设定类 | 多条背景设定 | 新增、删除、展开/收起、预览 |
| 势力 | 内置设定类 | 多条势力设定 | 新增、删除、展开/收起、预览 |
| 地点 | 内置设定类/新版自定义类型 | 多条地点设定 | 新增、删除、展开/收起、预览 |
| 物品 | 内置设定类/新版自定义类型 | 多条物品设定 | 新增、删除、展开/收起、预览 |
| 新分类 | 自定义 | 用户自定义设定类型 | 新增分类、删除分类 |

截图中的眼睛图标对应“预览”。箭头对应“展开/收起”。`+` 对应新增。

### 2.4 角色模块

角色一级分组：

```text
角色
  - 主要角色
  - 次要角色
```

角色条目显示格式：

```text
未命名        主要角色        [预览图标]
未命名        次要角色        [预览图标]
```

角色数据格式建议：

```json
{
  "id": "character_id",
  "name": "祥子",
  "gender": "男",
  "personality": "勤劳坚韧、单纯倔强",
  "simple_description": "",
  "detail_description": "",
  "dialogue": "能体现角色个性的台词",
  "character_label_ids": ["label_id"],
  "labels": [],
  "role": "protagonist",
  "background": "角色背景",
  "biography": "人物小传",
  "deleted": false,
  "deleted_at": null
}
```

角色类型：

| 值 | 中文 | 用途 |
|---|---|---|
| `protagonist` | 主要角色 | 主角、核心人物 |
| `supporting` | 次要角色 | 配角、反派、工具人、关系人物 |

角色应支持的操作：
- 新增主要角色
- 新增次要角色
- 编辑角色信息
- 删除/软删除角色
- 恢复已删除角色
- 预览角色
- 展开/收起角色分组
- 本地记忆展开状态
- AI 一键生成角色
- AI 生成人物小传

### 2.5 背景/势力/地点/物品模块

蛙蛙旧版明确有：

```text
background_settings: 背景设定
faction_settings: 势力设定
```

截图显示新版侧栏还扩展到：

```text
背景
势力
地点
物品
```

建议统一做成 `setting_types + settings` 模型，不要为每类单独建表。

设定类型格式：

```json
{
  "id": "location_setting",
  "name": "地点",
  "builtin": true,
  "deleted": false,
  "deleted_at": null
}
```

设定条目格式：

```json
{
  "id": "setting_id",
  "type_id": "location_setting",
  "name": "青云城",
  "title": "青云城",
  "content": "地点介绍、规则、历史、相关剧情",
  "related_roles": ["character_id"],
  "deleted": false,
  "deleted_at": null
}
```

内置类型建议：

| type_id | 中文名 | 图标建议 | 说明 |
|---|---|---|---|
| `background_setting` | 背景 | background icon | 世界背景、时代、规则 |
| `faction_setting` | 势力 | faction icon | 门派、公司、组织、阵营 |
| `location_setting` | 地点 | location icon | 城市、房间、秘境、地图点 |
| `thing_setting` | 物品 | box icon | 道具、法宝、武器、线索物 |
| `custom_xxx` | 自定义分类 | custom icon | 用户自建，例如功法、时间线、案件 |

### 2.6 预览功能

截图中每个一级/子级条目右侧都有眼睛图标，蛙蛙前端埋点里有：

```text
NovelEditSidebarPreviewClick
previewItemEnum: ROLE | SETTING
role_id / setting_id
```

建议交互：
- 点击列表文字：进入编辑态。
- 点击眼睛：右侧弹出只读预览，不切换编辑焦点。
- 预览内容包含名称、类型、标签、正文、关联角色。

### 2.7 展开状态

蛙蛙会把展开状态存到 localStorage，按作品 ID 维度隔离：

```text
角色展开状态：novelCreate.roleExpanded.{base_novel_id}
设定类型展开状态：novelCreate.settingTypeExpanded.{base_novel_id}
```

建议 CC 复刻：

```json
{
  "roleSectionExpanded": true,
  "settingTypeExpanded": {
    "background_setting": true,
    "faction_setting": false,
    "location_setting": true,
    "thing_setting": true
  }
}
```

## 3. 星月写作：对应功能映射

星月没有同名“作品信息侧栏”，但底层功能更偏资料库：

| 星月能力 | 接口/功能 | 可映射到本项目 |
|---|---|---|
| 书籍信息 | `/v1/book/info` | 作品基本信息 |
| 章节列表 | `/v1/chapters` | 正文/章节树 |
| 章节摘要 | `/v1/chapter/summary`、`/v1/chapter/summary/update` | 章纲/章节摘要 |
| 书籍摘要 | `/v1/book/summary`、`/v2/book/summaries` | 总纲/作品摘要 |
| 角色库 | `/v1/characters`、`/v1/create/character`、`/v1/update/character` | 角色模块 |
| 词条库 | `/v1/glossary`、`/v1/create/glossary`、`/v1/update/glossary` | 设定/地点/物品/术语 |
| 角色目录 | `/v1/character/dirs`、`/v1/character/dir/create` | 角色分组 |
| 词条目录 | `/v1/glossary/dirs`、`/v1/glossary/dir/create` | 设定分类 |
| 备忘录 | `/v1/book/memos`、`/v1/book/memo/create` | 灵感/草稿/笔记 |
| AI 抽取 | `/v1/extract/character`、`/v1/extract/glossary` | 从正文自动抽取角色/设定 |

星月值得借鉴的功能：
- 角色和词条支持目录。
- 支持批量移动角色/词条。
- 支持从正文抽取角色/词条。
- 支持置顶和排序：`toggle-top`、`sort/character`、`sort/glossary`。
- 备忘录可以作为“草稿/灵感/临时设定”的补充层。

## 4. 推荐给 CC 的产品结构

### 4.1 左侧栏结构

```text
┌─────────────────────────┐
│ 作品信息 | 正文 | 草稿       │
├─────────────────────────┤
│ 主题                    │
│  [标签]             👁    │
│  [总纲]             👁    │
│─────────────────────────│
│ 设定               ＋     │
│  [角色]            ＋ ⌃   │
│    未命名  主要角色  👁   │
│    未命名  次要角色  👁   │
│  [背景]            ＋ ⌄   │
│  [势力]            ＋ ⌄   │
│  [地点]            ＋ ⌄   │
│  [物品]            ＋ ⌄   │
└─────────────────────────┘
```

### 4.2 内容区结构

点击不同菜单后，右侧编辑区显示：

| 点击项 | 右侧内容 |
|---|---|
| 标签 | 标签选择器，多分类标签 |
| 总纲 | 富文本总纲编辑器 + 思维导图 Tab |
| 角色分组 | 角色列表/关系图 |
| 单个角色 | 角色详情表单 |
| 背景/势力/地点/物品分组 | 该分类下的设定列表 |
| 单个设定 | 设定详情编辑器 |
| 正文 Tab | 卷/章节树 + 正文编辑器 |
| 草稿 Tab | 草稿列表 + 恢复/删除/继续编辑 |

## 5. 建议数据模型

### 5.1 Novel

```ts
type Novel = {
  id: string;
  title: string;
  storyType: "short" | "long" | "script";
  channel?: "男频" | "女频";
  pov?: "第一人称" | "第三人称";
  introduction?: string;
  labelIds: string[];
  wordCount: number;
  status: "normal" | "draft" | "archived";
  createdAt: string;
  updatedAt: string;
};
```

### 5.2 Outline

```ts
type Outline = {
  id: string;
  novelId: string;
  type: "general" | "chapter" | "volume";
  title: string;
  content: string;        // 富文本 HTML 或 Markdown
  mindmap?: unknown;      // 思维导图 JSON
  relatedRoleIds?: string[];
  createdAt: string;
  updatedAt: string;
};
```

### 5.3 Character

```ts
type Character = {
  id: string;
  novelId: string;
  name: string;
  role: "protagonist" | "supporting";
  gender?: string;
  personality?: string;
  background?: string;
  biography?: string;
  dialogue?: string;
  labelIds: string[];
  deleted: boolean;
  deletedAt?: string;
  sort: number;
};
```

### 5.4 SettingType

```ts
type SettingType = {
  id: string;
  novelId: string;
  name: string;
  builtin: boolean;
  icon: string;
  deleted: boolean;
  sort: number;
};
```

### 5.5 SettingItem

```ts
type SettingItem = {
  id: string;
  novelId: string;
  typeId: string;
  name: string;
  content: string;
  relatedRoleIds: string[];
  deleted: boolean;
  deletedAt?: string;
  sort: number;
};
```

### 5.6 Draft

```ts
type Draft = {
  id: string;
  novelId: string;
  title: string;
  content: string;
  sourceType: "chapter" | "outline" | "setting" | "free";
  sourceId?: string;
  folderId?: string;
  createdAt: string;
  updatedAt: string;
};
```

## 6. API 建议

### 基础读取

```http
GET /api/novels/:novelId/sidebar
```

返回：

```json
{
  "novel": {},
  "labels": [],
  "outlines": [],
  "characters": [],
  "settingTypes": [],
  "settings": [],
  "chapters": [],
  "drafts": []
}
```

### 角色

```http
POST /api/novels/:novelId/characters
PATCH /api/characters/:id
DELETE /api/characters/:id
POST /api/characters/:id/restore
POST /api/novels/:novelId/characters/sort
POST /api/novels/:novelId/characters/extract-from-text
```

### 设定

```http
POST /api/novels/:novelId/setting-types
PATCH /api/setting-types/:id
DELETE /api/setting-types/:id

POST /api/novels/:novelId/settings
PATCH /api/settings/:id
DELETE /api/settings/:id
POST /api/settings/:id/restore
POST /api/novels/:novelId/settings/sort
POST /api/novels/:novelId/settings/extract-from-text
```

### 总纲/标签/草稿

```http
PATCH /api/novels/:novelId/labels
POST /api/novels/:novelId/outlines
PATCH /api/outlines/:id
POST /api/novels/:novelId/drafts
PATCH /api/drafts/:id
DELETE /api/drafts/:id
POST /api/drafts/:id/restore-to-source
```

## 7. 交互细节要求

1. 左侧栏宽度固定，建议 280-320px；移动端改抽屉。
2. 顶部 Tab 使用分段控件，选中态明显。
3. 当前选中项使用浅色背景 + 左侧/边框高亮。
4. 右侧眼睛图标只做预览，不改变当前编辑状态。
5. `+` 在不同位置含义不同：
   - 设定标题旁：新增分类或打开新增菜单。
   - 角色旁：新增主要/次要角色菜单。
   - 分类旁：新增该分类条目。
6. 折叠状态按作品保存到 localStorage。
7. 删除采用软删除，至少保留恢复入口。
8. 空状态展示“暂无角色/暂无内容”，并提供新增按钮。
9. 所有条目默认名为“未命名”。
10. 角色、设定、总纲内容要能作为 AI 工作流上下文输入。

## 8. AI 关联能力

该模块不是纯资料管理，应接入 AI：

| 位置 | AI 能力 |
|---|---|
| 标签 | 根据简介/正文自动推荐标签 |
| 总纲 | 一键生成总纲、改写总纲、转思维导图 |
| 角色 | 一键生成主要/次要角色、生成人物小传、补全台词 |
| 背景 | 根据题材生成世界背景 |
| 势力 | 生成组织/阵营/公司/门派 |
| 地点 | 生成地图点、场景、秘境、城市 |
| 物品 | 生成道具、线索物、武器、法宝 |
| 正文 | 从章节中抽取角色和设定 |
| 草稿 | 将草稿归档到角色/设定/章节 |

AI 输出建议格式：

```json
{
  "items": [
    {
      "type": "character",
      "name": "角色名",
      "role": "supporting",
      "content": {
        "gender": "男",
        "personality": "...",
        "background": "...",
        "biography": "...",
        "dialogue": "..."
      }
    }
  ]
}
```

## 9. 开发优先级

### P0：必须完成

- 顶部 Tab：作品信息/正文/草稿
- 作品信息侧栏：标签、总纲、角色、背景、势力、地点、物品
- 角色：主要/次要分组、新增、编辑、预览、删除
- 设定：分类、新增条目、编辑、预览、删除
- 本地保存展开状态
- 右侧编辑表单

### P1：增强体验

- 拖拽排序
- 自定义设定分类
- 软删除恢复
- 从正文抽取角色/设定
- 总纲思维导图
- 角色/设定关联

### P2：AI 深度集成

- 一键生成角色
- 一键生成世界观/势力/地点/物品
- AI 检查设定一致性
- 角色关系图
- 设定作为工作流上下文变量

## 10. 给 CC 的一句话需求

请开发一个“作品资料库侧栏”，复刻蛙蛙写作截图中的 `作品信息/正文/草稿` 三 Tab 与 `标签/总纲/角色/背景/势力/地点/物品` 资料树；底层采用 `Novel + Outline + Character + SettingType + SettingItem + Draft` 数据模型，支持新增、编辑、预览、折叠、软删除、排序，并预留 AI 生成/抽取接口。
