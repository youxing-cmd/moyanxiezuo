# 九章写作 — V2/V3/V4 排期计划

> 同步日期：2026-05-12
> V2 大部分功能已超额完成（开发计划 W3-W5 实际全部已落地），剩余工作聚焦 L3 路由调优 + 端到端测试。V3/V4 为方向性规划。

---

## V2 已完成清单（仅供参考，已从待办中移除）

### W1-W2：L3 Agent 路由层
- ✅ `backend/src/services/agentRouter.ts` 路由服务（gemini-2.5-flash + 失败降级）
- ✅ `backend/src/routes/ai-agent.ts` 端点 `POST /api/ai/agent-chat`
- ✅ `backend/src/routes/ai.ts` 导出共享函数（buildWorkContextPrompt/STYLE_PROMPTS/streamResponse）
- ✅ `frontend/js/app.js` 灰度开关 `?agent=1` 切换到 agent-chat
- ✅ `CLAUDE.md` + `L3-agent-router.md` 文档

### W3：编辑器核心体验
- ✅ AI 纠错（detect）+ 去 AI 味（de-ai）— 前端已绑定 streamSSE
- ✅ 查找替换 — 完整面板，支持正则、大小写、本页/全部
- ✅ 导入作品 — 支持 txt/docx/doc，章节自动拆分，预览编辑

### W4：作品管理流程
- ✅ 作品详情页 — workDetail 路由，视角/频道/标签/简介/封面表单
- ✅ 灵感库 — 完整 CRUD + 回收站 + 搜索 + 分类筛选

### W5：登录与榜单
- ✅ 手机号验证码登录 — send-code + login-by-code 已对接，60 秒倒计时
- ✅ 飞书登录 — /auth/feishu/url 跳转授权（飞书开放平台 appId 配置在 backend/.env）
- ✅ 热门榜单 — 多平台 tab（抖音/微博/今日头条/B站等）+ 日期切换 + 九州榜单

**注**：以上功能在 V2 原生前端（`frontend/js/app.js`）中已经实现，开发计划-v1 严重过时。

---

## V2 剩余待办

### P0：L3 路由层验证

**目标**：跑测试确认路由准确率，必要时调 prompt。

| 任务 | 验收 |
|------|------|
| 跑 `L3-agent-router.md` 的 5 个关键测试 case | 准确率 ≥ 80%（4/5 选对模型和工具） |
| 端到端日志检查 | 路由日志出现 `[agent-chat] route: ...`，降级率 < 10% |
| 灰度对比 | 同一 query 加/不加 `?agent=1`，对比模型选择和成本 |

**阻塞条件**：V1 后端测试期占用端口 3000，需等 V1 测试完才能启动 V2 跑端到端。

### P1：用户测试 + bug 修复

**目标**：用户实际跑现有功能，反馈 bug，修复。

待用户反馈具体问题后再排期。

### P2：CLAUDE.md 同步

**目标**：CLAUDE.md 的 API 清单只列了 5 个 AI 端点，实际 V2 后端有 20+ 个；数据库 Schema 也不全。

| 任务 | 验收 |
|------|------|
| 补全 API 清单 | 列出所有 `/api/ai/*`、`/api/inspirations/*`、`/api/trends/*`、`/api/stats/*`、`/api/points/*` 等 |
| 补全数据库 Schema 描述 | 列出所有 14 张表（drafts、settings、submissions、modelConfigs、trends* 等） |

---

## V3：Agent 架构升级（待定）

**触发条件**：V2 L3 路由层稳定运行 2-4 周，准确率达标后启动。

**方向性规划（非承诺）**：

| 能力 | 说明 | 优先级 |
|------|------|--------|
| 路由结果入库 | 新增 `agent_routes` 表，记录每次路由的 query/decision/user_feedback | P0（V3 起步） |
| 路由结果缓存 | 同样的 query 直接复用最近一次决策（如 5 分钟内） | P1 |
| 用户偏好学习 | 记录用户拒绝路由后的修正，调整路由权重 | P1 |
| 多步规划 | "帮我写一章" 自动拆解为：读大纲→看角色→写开头→补冲突→收尾 | P2 |
| 自我反思评估 | AI 生成后自我评估质量，不达标自动重试 | P2 |
| Agent 状态机 | idle/observing/suggesting/acting 状态流转 | P2 |
| 主动建议 | 编辑器空闲检测剧情停滞/逻辑矛盾，主动提示 | P3 |

**预估工作量**：2-3 个月

---

## V4：生态扩展（待定）

**触发条件**：V3 验证 Agent 模式有用户价值后启动。预计最早 2026 Q4 或 2027。

**方向性规划（非承诺）**：

| 方向 | 说明 |
|------|------|
| 多用户协作 | 同一作品多人编辑，AI 协调冲突 |
| 插件/模板市场 | 用户自定义提示词、工具、工作流 |
| 数据洞察 | 写作数据分析：速度曲线、情绪分布、钩子密度 |
| 出版对接 | 一键导出到各大网文平台格式 |
| API 开放 | 第三方应用调用九章 AI 能力 |

---

## 风险与依赖

| 风险 | 影响 | 缓解 |
|------|------|------|
| V1 后端测试期占用端口 3000 | V2 无法端到端验证 | 等 V1 测试完再做 P0 |
| L3 路由准确率不达标 | 用户感知不到 L3 价值 | 准确率 < 80% 时调 `agentRouter.ts:buildRouterPrompt()` 规则 |
| 网宿网关 gemini-2.5-flash 稳定性 | 降级率过高 | 监控降级率 > 10% 时考虑换路由模型 |

---

## 当前状态（截至 2026-05-12）

- V2 后端：~95% 完成（功能完整 + L3 路由层就绪）
- V2 前端：~90% 完成（原生前端，所有 PRD 功能已实现）
- V2 文档：~80%（CLAUDE.md 部分过时，需补全 API 清单和 Schema）
- V2 测试：0%（等 V1 测试期结束）
